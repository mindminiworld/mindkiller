local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera

-- ตัวแปรตั้งค่าต่างๆ
_G.AutoPunch = false
_G.PCPunch = false -- เพิ่มตัวแปรสำหรับ PC ONLY
_G.AutoEquipItems = false
_G.AutoSize2 = false 
_G.AntiKnockback = false
_G.HidePunchAnim = false
_G.SelectedTargets = {} 
_G.SpectatingPlayer = nil 
local startTime = os.time()

-- 🛡️ ระบบ Anti AFK
LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

if PlayerGui:FindFirstChild("Mindmain") then
    PlayerGui.GeminiProUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui", PlayerGui)
ScreenGui.Name = "Mindmain"
ScreenGui.ResetOnSpawn = false

-- 🎨 Main UI Design (Neon Dark Theme)
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 480, 0, 520) 
MainFrame.Position = UDim2.new(0.5, -240, 0.5, -260)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
MainFrame.BackgroundTransparency = 0.1
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.ClipsDescendants = true 
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

-- แสงนีออนรอบกรอบ
local NeonStroke = Instance.new("UIStroke", MainFrame)
NeonStroke.Color = Color3.fromRGB(255, 60, 60)
NeonStroke.Thickness = 2.5
NeonStroke.Transparency = 0.1

local TopBar = Instance.new("TextButton", MainFrame)
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 35)
TopBar.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
TopBar.BackgroundTransparency = 0.3
TopBar.BorderSizePixel = 0
TopBar.AutoButtonColor = false
TopBar.Text = ""
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 10)

local Header = Instance.new("TextLabel", TopBar)
Header.Size = UDim2.new(1, -30, 1, 0)
Header.Position = UDim2.new(0, 15, 0, 0)
Header.BackgroundTransparency = 1
Header.Text = "💪 ต่อยพวกไก่ๆควายๆ KDL KT | ดีอ้าย : " .. LocalPlayer.DisplayName
Header.TextColor3 = Color3.fromRGB(255, 255, 255)
Header.Font = Enum.Font.GothamBold
Header.TextSize = 15
Header.TextXAlignment = Enum.TextXAlignment.Left

-- ระบบลากเมนู
local dragging, dragInput, dragStart, startPos
local function update(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end
TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
TopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then update(input) end
end)

local MainContent = Instance.new("Frame", MainFrame)
MainContent.Size = UDim2.new(1, 0, 1, -35)
MainContent.Position = UDim2.new(0, 0, 0, 35)
MainContent.BackgroundTransparency = 1

local TabContainer = Instance.new("Frame", MainContent)
TabContainer.Size = UDim2.new(1, 0, 0, 35)
TabContainer.Position = UDim2.new(0, 0, 0, 10)
TabContainer.BackgroundTransparency = 1

local ContentArea = Instance.new("Frame", MainContent)
ContentArea.Size = UDim2.new(1, -20, 1, -60)
ContentArea.Position = UDim2.new(0, 10, 0, 55)
ContentArea.BackgroundTransparency = 1

-- ระบบย่อเมนู
local isMinimized = false
TopBar.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    if isMinimized then
        MainContent.Visible = false 
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, 480, 0, 35)}):Play()
    else
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, 480, 0, 520)}):Play()
        task.wait(0.3)
        if not isMinimized then MainContent.Visible = true end
    end
end)

local TabListLayout = Instance.new("UIListLayout", TabContainer)
TabListLayout.FillDirection = Enum.FillDirection.Horizontal
TabListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
TabListLayout.Padding = UDim.new(0, 6)

local LeftPanel = Instance.new("Frame", ContentArea)
LeftPanel.Size = UDim2.new(0.35, 0, 1, 0)
LeftPanel.BackgroundTransparency = 1
LeftPanel.Visible = false 

local RightPanel = Instance.new("Frame", ContentArea)
RightPanel.Size = UDim2.new(0.65, -8, 1, 0)
RightPanel.Position = UDim2.new(0.35, 8, 0, 0)
RightPanel.BackgroundTransparency = 1
RightPanel.Visible = false

-- ฟังก์ชันสร้างปุ่มเมนูด้านซ้าย
local function createFeatureBtn(name, defaultText, posOffset, onClick, globalVarName)
    local Btn = Instance.new("TextButton", LeftPanel)
    Btn.Size = UDim2.new(1, 0, 0, 45)
    Btn.Position = UDim2.new(0, 0, 0, posOffset)
    Btn.BackgroundColor3 = Color3.fromRGB(35, 15, 15)
    Btn.Text = defaultText
    Btn.TextColor3 = Color3.new(1, 1, 1)
    Btn.Font = Enum.Font.GothamBold
    Btn.TextSize = 14
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 8)
    
    local Stroke = Instance.new("UIStroke", Btn)
    Stroke.Color = Color3.fromRGB(180, 50, 50)
    Stroke.Thickness = 1.2

    Btn.MouseButton1Click:Connect(function()
        _G[globalVarName] = not _G[globalVarName]
        onClick(Btn, Stroke, _G[globalVarName])
    end)
end

-- สร้างปุ่มต่างๆ
createFeatureBtn("AutoPunch", " [ ปิด ]\nต่อยพวกโง่", 0, function(Btn, Stroke, isOn)
    Btn.Text = isOn and " [ เปิด ]\nต่อยพวกโง่" or " [ ปิด ]\nต่อยพวกโง่"
    Btn.TextColor3 = isOn and Color3.fromRGB(50, 255, 100) or Color3.new(1, 1, 1)
    Stroke.Color = isOn and Color3.fromRGB(50, 255, 100) or Color3.fromRGB(180, 50, 50)
end, "AutoPunch")

-- 🔥 ปุ่มใหม่ PC ONLY (ดึงสคริปต์ V1 มาใส่)
createFeatureBtn("PCPunch", " [ ปิด ]\nPC ONLY", 55, function(Btn, Stroke, isOn)
    Btn.Text = isOn and " [ เปิด ]\nPC ONLY" or " [ ปิด ]\nPC ONLY"
    Btn.TextColor3 = isOn and Color3.fromRGB(50, 255, 100) or Color3.new(1, 1, 1)
    Stroke.Color = isOn and Color3.fromRGB(50, 255, 100) or Color3.fromRGB(180, 50, 50)
end, "PCPunch")

createFeatureBtn("AutoEquip", " [ ปิด ]\nถือไข่ + Nan", 110, function(Btn, Stroke, isOn)
    Btn.Text = isOn and " [ เปิด ]\nถือไข่ + Nan" or " [ ปิด ]\nถือไข่ + Nan"
    Btn.TextColor3 = isOn and Color3.fromRGB(50, 255, 100) or Color3.new(1, 1, 1)
    Stroke.Color = isOn and Color3.fromRGB(50, 255, 100) or Color3.fromRGB(180, 50, 50)
end, "AutoEquipItems")

createFeatureBtn("AutoSize", " [ ปิด ]\nปรับขนาด 2", 165, function(Btn, Stroke, isOn)
    Btn.Text = isOn and " [ เปิด ]\nปรับขนาด 2" or " [ ปิด ]\nปรับขนาด 2"
    Btn.TextColor3 = isOn and Color3.fromRGB(50, 255, 100) or Color3.new(1, 1, 1)
    Stroke.Color = isOn and Color3.fromRGB(50, 255, 100) or Color3.fromRGB(180, 50, 50)
end, "AutoSize2")

createFeatureBtn("AntiKB", " [ ปิด ]\nกันกระเด็น", 220, function(Btn, Stroke, isOn)
    Btn.Text = isOn and " [ เปิด ]\nกันกระเด็น" or " [ ปิด ]\nกันกระเด็น"
    Btn.TextColor3 = isOn and Color3.fromRGB(50, 255, 100) or Color3.new(1, 1, 1)
    Stroke.Color = isOn and Color3.fromRGB(50, 255, 100) or Color3.fromRGB(180, 50, 50)
end, "AntiKnockback")

local function ToggleNoAnim(isOn)
    if isOn then
        local blockedAnimations = {
            ["rbxassetid://3638729053"] = true,
            ["rbxassetid://3638767427"] = true,
        }

        local function setupAnimationBlocking()
            local char = LocalPlayer.Character
            if not char or not char:FindFirstChild("Humanoid") then return end
            local humanoid = char:FindFirstChild("Humanoid")

            for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
                if track.Animation then
                    local animId = track.Animation.AnimationId
                    local animName = track.Name:lower()
                    if blockedAnimations[animId] or animName:match("punch") or animName:match("attack") or animName:match("right") then
                        track:Stop()
                    end
                end
            end

            if not _G.AnimBlockConnection then
                _G.AnimBlockConnection = humanoid.AnimationPlayed:Connect(function(track)
                    if track.Animation then
                        local animId = track.Animation.AnimationId
                        local animName = track.Name:lower()
                        if blockedAnimations[animId] or animName:match("punch") or animName:match("attack") or animName:match("right") then
                            track:Stop()
                        end
                    end
                end)
            end
        end

        setupAnimationBlocking()

        if not _G.AnimMonitorConnection then
            _G.AnimMonitorConnection = RunService.Heartbeat:Connect(function()
                if tick() % 0.5 < 0.01 then
                    local char = LocalPlayer.Character
                    if char and char:FindFirstChild("Humanoid") then
                        for _, track in pairs(char.Humanoid:GetPlayingAnimationTracks()) do
                            if track.Animation then
                                local animId = track.Animation.AnimationId
                                local animName = track.Name:lower()
                                if blockedAnimations[animId] or animName:match("punch") or animName:match("attack") or animName:match("right") then
                                    track:Stop()
                                end
                            end
                        end
                    end
                end
            end)
        end

        if not _G.CharacterAddedConnection then
            _G.CharacterAddedConnection = LocalPlayer.CharacterAdded:Connect(function(newChar)
                task.wait(1)
                setupAnimationBlocking()
            end)
        end
    else
        if _G.AnimBlockConnection then _G.AnimBlockConnection:Disconnect() _G.AnimBlockConnection = nil end
        if _G.AnimMonitorConnection then _G.AnimMonitorConnection:Disconnect() _G.AnimMonitorConnection = nil end
        if _G.CharacterAddedConnection then _G.CharacterAddedConnection:Disconnect() _G.CharacterAddedConnection = nil end
    end
end

createFeatureBtn("NoAnim", " [ ปิด ]\n(ลบอนิเมชั่น)", 275, function(Btn, Stroke, isOn)
    Btn.Text = isOn and " [ เปิด ]\n(ลบอนิเมชั่น)" or " [ ปิด ]\n(ลบอนิเมชั่น)"
    Btn.TextColor3 = isOn and Color3.fromRGB(50, 255, 100) or Color3.new(1, 1, 1)
    Stroke.Color = isOn and Color3.fromRGB(50, 255, 100) or Color3.fromRGB(180, 50, 50)
    ToggleNoAnim(isOn)
end, "HidePunchAnim")

-- Tab System
local pages = {}
local tabButtons = {}
local function createTab(name, width)
    local TabBtn = Instance.new("TextButton", TabContainer)
    TabBtn.Size = UDim2.new(0, width or 140, 1, 0)
    TabBtn.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
    TabBtn.Text = name
    TabBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    TabBtn.Font = Enum.Font.GothamBold
    TabBtn.TextSize = 14
    Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 8)

    local Page = Instance.new("ScrollingFrame", ContentArea)
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.BorderSizePixel = 0
    Page.ScrollBarThickness = 3
    Page.ScrollBarImageColor3 = Color3.fromRGB(255, 60, 60)
    Page.Visible = false
    Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    
    local PageLayout = Instance.new("UIListLayout", Page)
    PageLayout.Padding = UDim.new(0, 8)

    table.insert(pages, Page)
    table.insert(tabButtons, TabBtn)

    TabBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(pages) do p.Visible = false end
        for _, b in pairs(tabButtons) do 
            b.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
            b.TextColor3 = Color3.fromRGB(200, 200, 200)
        end
        Page.Visible = true
        TabBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
        TabBtn.TextColor3 = Color3.new(1, 1, 1)
        
        if name == "ล็อคพวกโง่" then
            LeftPanel.Visible = true
            RightPanel.Visible = true
            Page.Parent = RightPanel
        else
            LeftPanel.Visible = false
            RightPanel.Visible = false
            Page.Parent = ContentArea
        end
    end)
    return Page
end

local PageTarget = createTab("ล็อคพวกโง่", 130)
local PageSpectate = createTab("ส่องพวกโง่", 130)
local PageStats = createTab("อื่นๆ", 130)

-- หน้าแรกเริ่มต้น
pages[1].Parent = RightPanel
pages[1].Visible = true
LeftPanel.Visible = true
RightPanel.Visible = true
tabButtons[1].BackgroundColor3 = Color3.fromRGB(200, 40, 40)
tabButtons[1].TextColor3 = Color3.new(1, 1, 1)

-- ฟังก์ชัน Refresh List ผู้เล่น
local function refreshLists()
    for _, child in pairs(PageTarget:GetChildren()) do
        if child:IsA("TextButton") or child:IsA("TextLabel") then child:Destroy() end
    end
    for _, child in pairs(PageSpectate:GetChildren()) do
        if child:IsA("TextButton") or child:IsA("TextLabel") then child:Destroy() end
    end

    local lockLabel = Instance.new("TextLabel", PageTarget)
    lockLabel.Size = UDim2.new(1, 0, 0, 25)
    lockLabel.BackgroundTransparency = 1
    lockLabel.Text = "เลือกชื่อเพื่อพวกโง่ต่อยแม่งเลย"
    lockLabel.TextColor3 = Color3.fromRGB(255, 150, 150)
    lockLabel.Font = Enum.Font.GothamBold
    lockLabel.TextSize = 14

    local ClearLockBtn = Instance.new("TextButton", PageTarget)
    ClearLockBtn.Size = UDim2.new(1, 0, 0, 35)
    ClearLockBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
    ClearLockBtn.Text = "เคลียร์ชื่อพวกโง่"
    ClearLockBtn.TextColor3 = Color3.new(1, 1, 1)
    ClearLockBtn.Font = Enum.Font.GothamBold
    ClearLockBtn.TextSize = 14
    Instance.new("UICorner", ClearLockBtn).CornerRadius = UDim.new(0, 8)
    ClearLockBtn.MouseButton1Click:Connect(function()
        _G.SelectedTargets = {}
        refreshLists()
    end)

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local lockBtn = Instance.new("TextButton", PageTarget)
            lockBtn.Size = UDim2.new(1, 0, 0, 35)
            local isLocked = _G.SelectedTargets[p.UserId]
            lockBtn.BackgroundColor3 = isLocked and Color3.fromRGB(180, 40, 40) or Color3.fromRGB(40, 20, 20)
            lockBtn.Text = (isLocked and "🎯 " or "") .. p.DisplayName
            lockBtn.TextColor3 = Color3.new(1, 1, 1)
            lockBtn.Font = Enum.Font.GothamMedium
            lockBtn.TextSize = 14
            Instance.new("UICorner", lockBtn).CornerRadius = UDim.new(0, 6)
            lockBtn.MouseButton1Click:Connect(function()
                _G.SelectedTargets[p.UserId] = not _G.SelectedTargets[p.UserId]
                refreshLists()
            end)

            local specBtn = Instance.new("TextButton", PageSpectate)
            specBtn.Size = UDim2.new(1, 0, 0, 35)
            local isSpec = (_G.SpectatingPlayer == p)
            specBtn.BackgroundColor3 = isSpec and Color3.fromRGB(200, 80, 40) or Color3.fromRGB(40, 20, 20)
            specBtn.Text = (isSpec and "👁️ " or "") .. p.DisplayName
            specBtn.TextColor3 = Color3.new(1, 1, 1)
            specBtn.Font = Enum.Font.GothamMedium
            specBtn.TextSize = 15
            Instance.new("UICorner", specBtn).CornerRadius = UDim.new(0, 6)
            specBtn.MouseButton1Click:Connect(function()
                if _G.SpectatingPlayer == p then
                    _G.SpectatingPlayer = nil
                    local char = LocalPlayer.Character
                    if char and char:FindFirstChild("Humanoid") then Camera.CameraSubject = char.Humanoid end
                else
                    _G.SpectatingPlayer = p
                end
                refreshLists()
            end)
        end
    end
end

Players.PlayerRemoving:Connect(refreshLists)
Players.PlayerAdded:Connect(refreshLists)
refreshLists()

-- Stats Page
local TimeLabel = Instance.new("TextLabel", PageStats)
TimeLabel.Size = UDim2.new(1, 0, 0, 50)
TimeLabel.BackgroundTransparency = 1
TimeLabel.TextColor3 = Color3.fromRGB(150, 255, 150)
TimeLabel.Font = Enum.Font.GothamBlack
TimeLabel.TextSize = 18

local EmoteBtn = Instance.new("TextButton", PageStats)
EmoteBtn.Size = UDim2.new(1, 0, 0, 45)
EmoteBtn.BackgroundColor3 = Color3.fromRGB(35, 15, 15)
EmoteBtn.Text = "สคริปต์ท่าเต้นทั้งหมด"
EmoteBtn.TextColor3 = Color3.new(1, 1, 1)
EmoteBtn.Font = Enum.Font.GothamBold
EmoteBtn.TextSize = 14
Instance.new("UICorner", EmoteBtn).CornerRadius = UDim.new(0, 8)

local StrokeE = Instance.new("UIStroke", EmoteBtn)
StrokeE.Color = Color3.fromRGB(180, 50, 50)
StrokeE.Thickness = 1.5

EmoteBtn.MouseButton1Click:Connect(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/7yd7/Hub/refs/heads/Branch/GUIS/Emotes.lua"))()
    end)
    EmoteBtn.Text = "กำลังรันท่าเต้น"
    task.wait(1.5)
    EmoteBtn.Text = "สคริปต์ท่าเต้นทั้งหมด"
end)

local CloseBtn = Instance.new("TextButton", PageStats)
CloseBtn.Size = UDim2.new(1, 0, 0, 45)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
CloseBtn.Text = "🛑 ปิดสคริปต์"
CloseBtn.TextColor3 = Color3.new(1, 1, 1)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 16
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 8)

task.spawn(function()
    while task.wait(1) do
        if not ScreenGui.Parent then break end
        local diff = os.time() - startTime
        TimeLabel.Text = string.format("⏱️ เวลาฟาร์ม: %02dm %02ds", math.floor(diff/60), diff%60)
    end
end)

-- ระบบปิดการทำงานไข่
task.spawn(function()
    while task.wait(0.5) do
        local egg = (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Protein Egg")) or LocalPlayer.Backpack:FindFirstChild("Protein Egg")
        if egg and egg:IsA("Tool") then
            egg.Enabled = false 
        end
    end
end)

local muscleEvent = LocalPlayer:WaitForChild("muscleEvent", 10)
local remotePath = game:GetService("ReplicatedStorage"):WaitForChild("rEvents"):WaitForChild("changeSpeedSizeRemote")

-- LOOP สำหรับ ถือไอเทม, อนิเมชั่นต่อยหลอกๆ และ ขนาด Nan
task.spawn(function()
    while task.wait(0.05) do
        local char = LocalPlayer.Character
        local backpack = LocalPlayer:FindFirstChild("Backpack")
        
        if char and char:FindFirstChild("Humanoid") then
            if _G.AutoEquipItems then
                if backpack then
                    if not char:FindFirstChild("Punch") then
                        local punchTool = backpack:FindFirstChild("Punch")
                        if punchTool then punchTool.Parent = char end 
                    end
                    if not char:FindFirstChild("Protein Egg") then
                        local eggTool = backpack:FindFirstChild("Protein Egg")
                        if eggTool then eggTool.Parent = char end
                    end
                end
                pcall(function() remotePath:InvokeServer("changeSize", 0/0) end)
            end

            if _G.AutoPunch or _G.PCPunch then
                if not _G.AutoEquipItems and not char:FindFirstChild("Punch") and backpack then
                    local punchTool = backpack:FindFirstChild("Punch")
                    if punchTool then char.Humanoid:EquipTool(punchTool) end
                end
                if muscleEvent then
                    muscleEvent:FireServer("punch", "leftHand")
                    muscleEvent:FireServer("punch", "rightHand")
                end
            end
        end
    end
end)

-- LOOP สำหรับ Auto Size 2
task.spawn(function()
    while task.wait(1) do
        if _G.AutoSize2 then
            pcall(function() remotePath:InvokeServer("changeSize", 2) end)
        end
    end
end)

-- 🗿 ระบบ Anti Knockback
local kbConnection = RunService.Stepped:Connect(function()
    if _G.AntiKnockback then
        pcall(function()
            local char = LocalPlayer.Character
            if char then
                for _, part in pairs(char:GetChildren()) do
                    if part:IsA("BasePart") then
                        for _, v in pairs(part:GetChildren()) do
                            if v:IsA("BodyVelocity") or v:IsA("BodyGyro") or v:IsA("BodyPosition") or v:IsA("BodyThrust") or v:IsA("BodyAngularVelocity") or v:IsA("RocketPropulsion") or v:IsA("VectorForce") or v:IsA("LinearVelocity") then
                                v:Destroy()
                            end
                        end
                    end
                end
            end
        end)
    end
end)

-- ⚡ ระบบต่อยระยะไกล (ดึงเป้ามาหาเรา) ⚡
task.spawn(function()
    while true do
        if _G.AutoPunch then
            pcall(function()
                local char = LocalPlayer.Character
                local myArm = char and (char:FindFirstChild("RightHand") or char:FindFirstChild("Right Arm"))
                
                if myArm and myArm:IsDescendantOf(workspace) then
                    for _, targetPlayer in pairs(Players:GetPlayers()) do
                        if _G.SelectedTargets[targetPlayer.UserId] and targetPlayer.Character then
                            local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                            if targetRoot and targetRoot:IsDescendantOf(workspace) then
                                for _ = 1, 2 do
                                    firetouchinterest(targetRoot, myArm, 0)
                                    task.wait(0.01)
                                    firetouchinterest(targetRoot, myArm, 1)
                                end
                            end
                        end
                    end
                end
            end)
            task.wait(0.08) 
        else
            task.wait(0.1)
        end
    end
end)

-- ⚡ ระบบต่อย PC ONLY (ดึงมาจาก Kill V1) ⚡
local pcPunchConnection = RunService.RenderStepped:Connect(function()
    if _G.PCPunch then
        for _, targetPlayer in pairs(Players:GetPlayers()) do
            if _G.SelectedTargets[targetPlayer.UserId] and targetPlayer.Character then
                local targetPart = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                local myArm = LocalPlayer.Character and (LocalPlayer.Character:FindFirstChild("RightHand") or LocalPlayer.Character:FindFirstChild("Right Arm"))
                if targetPart and myArm then
                    firetouchinterest(targetPart, myArm, 0)
                    firetouchinterest(targetPart, myArm, 1)
                end
            end
        end
    end
end)

-- 👁️ ระบบส่องหน้าจอ
local runConnection = RunService.RenderStepped:Connect(function()
    if _G.SpectatingPlayer and _G.SpectatingPlayer.Character then
        local targetHum = _G.SpectatingPlayer.Character:FindFirstChild("Humanoid")
        if targetHum then Camera.CameraSubject = targetHum end
    end
end)

-- ปุ่มปิดสคริปต์
CloseBtn.MouseButton1Click:Connect(function()
    _G.AutoPunch = false
    _G.PCPunch = false
    _G.AutoEquipItems = false
    _G.AutoSize2 = false
    _G.AntiKnockback = false
    _G.HidePunchAnim = false
    ToggleNoAnim(false) 
    _G.SpectatingPlayer = nil
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then Camera.CameraSubject = char.Humanoid end
    runConnection:Disconnect()
    pcPunchConnection:Disconnect()
    kbConnection:Disconnect()
    ScreenGui:Destroy()
end)
